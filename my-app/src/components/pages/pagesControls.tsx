export default function PagesControls() {
return(<>contrl</>)}
   /* function nextPage() {
    props.setPage(page + 1)
}

function previousPage() {
    if (page>1){
        props.setPage(page - 1)
    }
}
props: {currentPage: number, setPage: number, allPages: number }
    return(
        <button onClick={nextPage}>next</button>
        <button onClock={previousPage}>previous</button>
    )
}*/